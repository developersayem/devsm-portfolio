/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['api.daily.dev'], // Add the domain here
      },
      // other Next.js configuration options...
};

export default nextConfig;
